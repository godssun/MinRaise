spring:
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://database-1.cly0soekmaaf.ap-northeast-2.rds.amazonaws.com:3306/minimum_raise_db
    username: root
    password: Kskdij12!!
    hikari:
      maximum-pool-size: 10
      connection-timeout: 30000 # 연결 타임아웃 30초
      idle-timeout: 600000 # 10분
      max-lifetime: 1800000 # 30분

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        logging:
          config: classpath:logback-spring.xml
          level:
            org:
              hibernate:
                SQL: DEBUG

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
